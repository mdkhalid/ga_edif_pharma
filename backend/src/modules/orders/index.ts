/**
 * Public API of the `orders` module.
 *
 * Only the Nest module is exported. Imports from outside must go through
 * this file (enforced by `scripts/check-module-boundaries.mjs`).
 */

export { OrdersModule } from './orders.module';
