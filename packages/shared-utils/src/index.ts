/**
 * @medichain/shared-utils
 *
 * Pure, dependency-light helpers shared by the backend, the website, the admin
 * portal and the mobile app. Nothing here may touch Node built-ins or the DOM —
 * React Native consumes this package too.
 */

export * from './money';
export * from './salt/normalize';
