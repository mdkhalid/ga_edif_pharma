/**
 * @medichain/shared-types
 *
 * Enums, DTO interfaces and transport shapes shared by the backend, website,
 * admin portal and mobile app. Pure types and constants only — no runtime
 * dependencies, no decorators, so React Native can import this freely.
 */

export * from './enums';
export * from './auth';
export * from './common';
