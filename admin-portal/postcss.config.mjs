/**
 * PostCSS config for Tailwind CSS v4 — see the website's copy for the rationale.
 * The plugin moved into `@tailwindcss/postcss` and the theme lives in CSS.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
